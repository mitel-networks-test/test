# Copilot Application Dependencies
# This file lists the required Python packages for the application

# No external dependencies required for the basic functionality
# The application uses only Python standard library modules:
# - argparse (command-line parsing)
# - logging (logging functionality)
# - sys (system-specific parameters)
# - os (operating system interface)
# - typing (type hints)
# - platform (platform identification)

# AWS Bedrock Agent Dependencies
boto3>=1.34.0              # AWS SDK for Python
botocore>=1.34.0           # Low-level interface to AWS services
requests>=2.31.0           # HTTP library for API calls
python-dotenv>=1.0.0       # Environment variable management
pyyaml>=6.0.1              # YAML configuration files